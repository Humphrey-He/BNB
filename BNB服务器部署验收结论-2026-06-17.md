# BNB 服务器部署验收结论

日期：2026-06-17  
服务器：`101.43.127.178`  
域名：`bnb.shuhong.icu`

## 1. 验收范围

本次验收覆盖以下内容：

- HTTPS 状态检查
- OpenResty 配置与暴露面检查
- Go API 与 Rust Node 监听范围检查
- 当前公网访问状态确认

---

## 2. 总体结论

当前 BNB 项目已经完成基础部署，并且可以通过次级域名 `http://bnb.shuhong.icu` 访问前端与 API。

但从“可访问”到“部署收口完成”之间，还存在明显差距。当前状态更适合演示环境，不适合直接视为完成生产化部署。

本次结论如下：

- HTTP 访问：通过
- 次级域名访问：通过
- OpenResty 反代：通过
- Go API 服务：运行正常
- Rust Node 服务：进程运行，但日志持续报错
- HTTPS：未正确完成
- 最小暴露：未完成

---

## 3. 当前可用项

### 3.1 域名访问

已确认下列访问可用：

- `http://bnb.shuhong.icu/`
- `http://bnb.shuhong.icu/api/v1/chains`

说明：

- Cloudflare DNS 中 `bnb.shuhong.icu` 已配置为 A 记录，指向 `101.43.127.178`
- OpenResty 当前对 `bnb.shuhong.icu` 提供了站点服务

### 3.2 服务进程

已确认：

- `asset-platform-api.service`：`active (running)`
- `chain-node.service`：`active (running)`

### 3.3 OpenResty 配置

已确认 OpenResty 配置文件存在：

- `/opt/1panel/www/conf.d/bnb.conf`

当前站点配置包含：

- `/` -> 前端静态资源
- `/api/` -> `172.17.0.1:8080`
- `/node/` -> `172.17.0.1:8081/rpc/`
- `/health` -> 本地返回 `ok`

---

## 4. HTTPS 检查结论

### 4.1 检查结果

当前 443 端口处于监听状态，但 HTTPS 并未正确可用。

实际表现：

- `http://bnb.shuhong.icu/` 返回 `200 OK`
- `https://bnb.shuhong.icu/` TLS 握手失败

本地验证结果显示：

- `curl.exe -k -I https://bnb.shuhong.icu/` 返回 TLS/SSL 握手错误
- 服务端无法正常提供可读取证书链

### 4.2 结论

当前服务器不能视为“已正确部署 HTTPS”。

更准确地说：

- 443 端口已经打开
- 但并没有完成对应域名证书与 HTTPS 站点配置闭环

### 4.3 风险

- 用户访问 `https://bnb.shuhong.icu` 时可能失败
- 无法满足正式分享、面试演示中的 HTTPS 预期
- 如果未来启用 Cloudflare 代理或浏览器自动升级 HTTPS，可能出现异常体验

---

## 5. OpenResty 最小暴露检查结论

### 5.1 当前配置情况

`bnb.conf` 当前内容核心如下：

- `listen 80 default_server;`
- `server_name bnb.shuhong.icu 101.43.127.178;`
- 反代 `/api/` 到 `172.17.0.1:8080`
- 反代 `/node/` 到 `172.17.0.1:8081/rpc/`

### 5.2 当前优点

- 站点入口已统一收敛到 OpenResty
- API 与节点能力可通过统一入口暴露
- 没有额外发现同目录下其它抢占 80 的自定义站点配置

### 5.3 当前问题

OpenResty 只能算“功能正确”，不能算“最小暴露完成”。

原因如下：

- 仅配置了 80 站点，没有完整 HTTPS server block
- `/node/` 直接暴露了 Rust 节点 RPC 能力
- 未看到更细粒度的访问控制、限流、来源限制或鉴权收口
- 没有看到额外安全头、HTTPS 跳转或更严格的反代限制

### 5.4 结论

OpenResty 当前配置能用，但还不满足“最小暴露”的安全要求。

---

## 6. Go / Rust 端口监听检查结论

### 6.1 当前监听状态

当前实际监听如下：

- `0.0.0.0:80` -> OpenResty
- `0.0.0.0:443` -> OpenResty
- `*:8080` -> Go API
- `0.0.0.0:8081` -> Rust Node

### 6.2 Go API

Go API 当前由 `asset-platform-api.service` 启动，应用日志显示：

- `Listening and serving HTTP on :8080`

这意味着 Go API 并不是只监听本地回环地址，而是对外网卡开放。

结论：

- 当前 Go API 不应继续保持公网监听
- 更合理的目标应为 `127.0.0.1:8080`

### 6.3 Rust Node

`chain-node.service` 当前明确配置：

- `Environment=RPC_PORT=8081`
- `Environment=RPC_BIND=0.0.0.0`

这意味着 Rust Node RPC 明确对公网开放。

结论：

- 当前 Rust Node 不应继续保持 `0.0.0.0:8081`
- 如果只是给 OpenResty 反代使用，应优先改为仅内网可达
- 如果 OpenResty 容器必须经 Docker bridge 访问宿主机，可结合防火墙限制来源，而不是完全裸露

### 6.4 防火墙状态

已确认：

- `ufw` 为 `inactive`
- `iptables INPUT` 默认接受流量

这意味着：

- `8080`
- `8081`
- `5432`
- `6379`

当前都存在较大暴露风险

---

## 7. 运行质量观察

### 7.1 Go API

Go API 当前健康检查通过：

- `http://127.0.0.1:8080/healthz` 返回 `{"status":"ok"}`

说明当前 API 服务本身运行正常。

### 7.2 Rust Node

虽然 `chain-node.service` 显示为 `active`，但日志持续出现：

`Block production failed: Parent block not found`

这说明：

- 进程存活
- 但业务逻辑并不健康

因此 Rust Node 当前应视为“服务启动成功，但功能状态异常”。

---

## 8. 验收结论分级

### 8.1 已通过

- 次级域名 HTTP 访问通过
- OpenResty 前端静态服务通过
- OpenResty `/api/` 反代通过
- Go API 服务运行通过

### 8.2 部分通过

- Rust Node 进程启动通过，但业务健康度未通过
- OpenResty 可用，但最小暴露未通过

### 8.3 未通过

- HTTPS 未通过
- Go / Rust 端口仅内网监听未通过
- 防火墙收口未通过

---

## 9. 建议整改项

### P0

- 为 `bnb.shuhong.icu` 正式配置 HTTPS 证书，并完成 80 -> 443 跳转
- 将 Go API 从公网监听收口到仅本地或仅反代可达
- 将 Rust Node 从 `0.0.0.0:8081` 收口，至少结合防火墙限制来源

### P1

- 为 OpenResty 增加更明确的反代边界与安全控制
- 评估是否应该对 `/node/` 路径继续公网开放
- 为服务器启用最基础的主机防火墙策略

### P2

- 继续排查 Rust Node 的 `Parent block not found` 异常
- 为部署状态建立一份持续更新的运行手册

---

## 10. 最终结论

截至 2026-06-17，BNB 项目已经达成“可通过次级域名访问的演示环境”目标，但尚未达到“安全收口完成的稳定部署环境”标准。

可以这样概括当前状态：

- 演示可用：是
- HTTP 可访问：是
- 域名入口正常：是
- HTTPS 完成：否
- 暴露面收口完成：否
- 节点功能健康：否

因此，当前服务器部署状态应标记为：

**“基础部署完成，公网演示可用，安全与网络收口待完成。”**
