# Sepolia 链路审查与最小运行测试

日期：2026-06-17  
项目：`web3-blockchain-backend-engineer`  
目标：审查 `scanner / parser / confirm-worker` 可运行程度，补齐最小运行入口，部署到服务器，并尝试完成一次真实 Sepolia 充值监听测试。

## 1. 总结结论

这次工作没有完全跑通“真实充值监听闭环”，但已经把项目从“只有内部包实现”推进到了“具备最小运行骨架并已在线启动”的状态。

结论可以概括为：

- 代码不是空壳，核心扫描/解析/确认逻辑是存在的
- 原始仓库缺少真正的进程入口，不能直接部署运行
- 已补齐 `scanner / parser / confirm-worker` 三个启动入口
- 已补齐数据库和 NATS 装配层
- 已修复一批会直接阻断扫描的底层问题
- 已把三个 worker 部署到服务器并启动成功
- 已种入一组真实 Sepolia 回放样本
- 但最终没有产生 `chain_events` / `deposits`
- 主要阻塞点是服务器侧对 Infura 的请求在 scanner 运行时超时

因此，本次状态应定义为：

**“最小运行链路已建立，真实 Sepolia 回放测试未完全成功，主要阻塞在服务器侧 RPC 连通性和项目现有架构的生产化缺口。”**

---

## 2. 审查结果

### 2.1 代码实现层面

以下内部模块已存在真实实现：

- `internal/scanner`
- `internal/parser`
- `internal/confirmworker`
- `internal/repository`
- `internal/rpcgateway`

这说明项目并不是概念仓库，而是已经有核心业务逻辑。

### 2.2 原始仓库缺失项

原始仓库存在几个关键缺口，导致它不能直接作为后台任务系统运行：

1. 没有 `cmd/scanner`
2. 没有 `cmd/parser`
3. 没有 `cmd/confirm-worker`
4. 没有数据库连接与 NATS 连接的公共装配层
5. API 当前仍主要是静态 stub，不是真实读库

### 2.3 架构层问题

本次审查还发现一个重要现实问题：

- 数据库表里的 `scan_checkpoints.chain_id`、`blocks.chain_id`、`deposits.chain_id` 等字段当前实际更像引用 `chains.id`
- 但部分代码又把它当作真实网络 `chain_id` 使用

当前线上数据库里：

- `chains.id = 1` 对应 `chain_id = 11155111`（Ethereum Sepolia）
- `chains.id = 2` 对应 `chain_id = 97`（BSC Testnet）

因此如果直接让 scanner 用 `11155111` 作为内部标识，会和现有表约束错位。

为了让最小测试先跑起来，本次临时策略是：

- worker 运行时使用数据库主键 `SCAN_CHAIN_DB_ID=1`
- RPC URL 单独指定为 Sepolia Infura

这能避开当前 schema/代码语义不一致的问题，但不是最终理想设计。

---

## 3. 本次代码改动

### 3.1 新增最小运行装配层

新增：

- `internal/app/bootstrap.go`

功能：

- 读取 Postgres 环境变量
- 建立数据库连接
- 建立 NATS 连接
- 创建基础 logger

### 3.2 新增三个命令入口

新增：

- `cmd/scanner/main.go`
- `cmd/parser/main.go`
- `cmd/confirm-worker/main.go`

作用：

- 让内部包首次具备真正可部署的独立进程入口

### 3.3 修复 scanner 底层问题

修复了 `internal/scanner/fetcher.go` 中几个关键问题：

1. `eth_getLogs` 调用参数错误
   - 原先把 filter 包装成了数组
   - 已改为直接传 filter object

2. 扫描批次区间存在 off-by-one
   - 已修正 `currentTo` 的计算

3. RPC 十六进制字段解码不完整
   - `timestamp`
   - `blockNumber`
   - `logIndex`
   已统一改为按十六进制解析

这些问题如果不修复，scanner 即使启动，也很难正确拿到链上日志。

### 3.4 新增数据库驱动依赖

新增：

- `github.com/lib/pq`

并执行了：

- `go mod tidy`

---

## 4. 部署动作

### 4.1 新编译二进制

本次新增并编译了 Linux AMD64 二进制：

- `scanner-linux-amd64`
- `parser-linux-amd64`
- `confirm-worker-linux-amd64`

### 4.2 上传到服务器

部署路径：

- `/home/ubuntu/opt/bnb/api/scanner-linux-amd64`
- `/home/ubuntu/opt/bnb/api/parser-linux-amd64`
- `/home/ubuntu/opt/bnb/api/confirm-worker-linux-amd64`

### 4.3 新增环境文件

新增服务器侧环境文件：

- `/home/ubuntu/opt/bnb/api/chain-workers.env`

核心变量：

- `POSTGRES_HOST=127.0.0.1`
- `POSTGRES_DB=asset_platform`
- `POSTGRES_USER=user_i2nZ7w`
- `NATS_URL=nats://127.0.0.1:4222`
- `SCAN_CHAIN_DB_ID=1`
- `SCAN_RPC_URL=https://sepolia.infura.io/v3/aa4778679f4e4e64a48621c2b6c0c8b8`

### 4.4 新增 systemd 服务

新增：

- `asset-platform-scanner.service`
- `asset-platform-parser.service`
- `asset-platform-confirm-worker.service`

---

## 5. 真实 Sepolia 回放测试设计

为了避免还要先手动打一笔新的测试币，本次没有采用“人工打款 -> 等待扫描”的方式，而是采用：

**“从 Sepolia 最近真实发生过的 ERC-20 Transfer 中选一笔做回放样本”**

选取样本：

- 合约地址：`0xaa8e23fb1079ea71e0a56f48a2aa51851d8433d0`
- 收款地址：`0x622e3e177386ca01ccc42115114cd56a9d2c21a0`
- 区块号：`11076996`

并将其写入数据库：

1. `tokens`
2. `watched_addresses`
3. `scan_checkpoints`

使 scanner 从该样本前一个区块开始回放。

---

## 6. 实际测试结果

### 6.1 服务启动状态

最终状态：

- `asset-platform-scanner`：已启动
- `asset-platform-parser`：已启动
- `asset-platform-confirm-worker`：已启动

说明：

- 最小运行骨架已经真正上线
- 不再是“只能在代码里阅读”的状态

### 6.2 数据结果

测试期间数据库结果：

- `chain_events = 0`
- `deposits = 0`

说明：

- scanner 没有成功把回放区块的日志打进下游链路
- parser 因为没收到 `raw_events`，只能持续超时等待

### 6.3 scanner 运行日志

关键失败日志：

`failed to get latest block: failed to get block number: Post "https://sepolia.infura.io/v3/...": context deadline exceeded`

这说明：

- scanner 启动成功
- 但在服务器运行时访问 Infura RPC 超时

这也是本次真实链路未能产出 `chain_events` / `deposits` 的直接原因。

---

## 7. 阻塞点归因

### 7.1 第一阻塞：服务器侧 Infura 连通性不稳定

最直接阻塞是：

- scanner 从服务器访问 Infura 时超时

这和之前服务器上对外访问部分 RPC 就不稳定的现象一致。

这不是前端问题，也不是数据库逻辑问题，而是：

- 出站网络
- DNS
- TLS
- 代理
- 或供应商链路质量

中的某一类问题。

### 7.2 第二阻塞：当前项目还不是完整生产型链上任务系统

即使 RPC 连通性恢复，项目仍然有几类生产化缺口：

1. API handlers 仍然是 stub
2. `chain_id` 语义在 DB 与代码之间不够统一
3. NATS 使用方式偏简单，消息确认契约还不够严谨
4. scanner/parser/confirm-worker 缺少更完整的健康指标与运维回路

所以它已经不是“纯展示页”，但还没到“资产平台正式主链路”的成熟度。

---

## 8. 当前阶段判断

如果从结果上判断，这次工作达成了三件非常重要的事：

1. 证明了项目可以继续往真实系统演进
2. 把链上任务系统从“内部包”推进成了“可部署 worker”
3. 把真正的阻塞点暴露到了基础设施层，而不再停留在概念层

这其实很关键，因为现在问题已经从：

- “项目能不能做”

转变成：

- “这条线上真实运行卡在哪”

这是一个质变。

---

## 9. 下一步建议

### P0

- 优先解决服务器访问 Infura / RPC 的超时问题
- 先只保留 Sepolia 单链闭环，不要同时追多链
- 完成一次真实 `chain_events -> deposits` 成功写库

### P1

- 统一 `chain_id` 在代码和数据库中的语义
- 把 API 从 stub 改成真实读库
- 为 scanner / parser / confirm-worker 增加独立 health 与 metrics

### P2

- 把测试从“回放样本”升级成“真实人工测试打款”
- 加入 ledger-service 的自动入账联动
- 补齐 reorg 与异常重试路径验证

---

## 10. 最终结论

截至 2026-06-17，本次工作没有完全跑通 Sepolia 真实充值监听闭环，但已经完成了最关键的基础推进：

- worker 入口已补齐
- Linux 二进制已编译
- 服务已部署并启动
- 回放样本已入库
- 真实阻塞点已定位到服务器访问 Infura 超时

因此当前阶段最准确的状态是：

**“从展示型项目推进到了可运行链路原型，但真实链上回放仍被服务器侧 RPC 连通性阻塞。”**
