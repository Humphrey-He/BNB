# BNB 服务器收口整改结果

日期：2026-06-17  
服务器：`101.43.127.178`  
域名：`bnb.shuhong.icu`

## 1. 本次整改目标

本次整改执行以下三项：

- 配置 HTTPS
- 收口 Go API 到仅内网可达
- 收口 Rust Node 到仅反代/受限访问

---

## 2. 已完成项

### 2.1 HTTP 跳转 HTTPS

已完成：

- `http://bnb.shuhong.icu/` 现在返回 `301`
- 自动跳转到 `https://bnb.shuhong.icu/`

### 2.2 HTTPS 站点已启用

已完成：

- OpenResty 增加了 443 HTTPS server
- 当前 `https://bnb.shuhong.icu/` 可访问
- 已添加响应头：
  - `Strict-Transport-Security`
  - `X-Content-Type-Options`
  - `X-Frame-Options`
  - `Referrer-Policy`

说明：

- 由于服务器当前无法访问 Let’s Encrypt，未能申请正式公网证书
- 当前 443 使用的是自签证书
- 因此浏览器仍可能出现“不受信任证书”警告

### 2.3 Go API 收口完成

已完成：

- Go API 新增 `BIND_ADDR` 支持
- 服务器当前配置为 `BIND_ADDR=172.17.0.1`
- 当前监听状态为：
  - `172.17.0.1:8080`

结论：

- Go API 已不再直接对公网监听
- 只能通过宿主机 Docker bridge 被 OpenResty 反代访问

### 2.4 Rust Node 收口完成

已完成：

- `chain-node.service` 中 `RPC_BIND` 已从 `0.0.0.0` 改为 `172.17.0.1`
- 当前监听状态为：
  - `172.17.0.1:8081`

结论：

- Rust Node 已不再直接对公网开放
- 当前仅供 OpenResty 通过 Docker bridge 反代使用

---

## 3. 当前验证结果

### 3.1 外部访问

已验证：

- `http://bnb.shuhong.icu/` -> `301 Moved Permanently`
- `https://bnb.shuhong.icu/` -> `200 OK`
- `https://bnb.shuhong.icu/api/v1/chains` -> 正常返回 JSON

### 3.2 监听端口

当前监听状态：

- `0.0.0.0:80` -> OpenResty
- `0.0.0.0:443` -> OpenResty
- `172.17.0.1:8080` -> Go API
- `172.17.0.1:8081` -> Rust Node

这说明：

- 80/443 作为公网入口保留
- 8080/8081 已完成收口

---

## 4. 未完成项

### 4.1 正式受信任 HTTPS 证书

未完成原因：

- 服务器访问 Let’s Encrypt 失败
- `certbot` 报错：`Network is unreachable`

当前影响：

- 技术上 HTTPS 已可用
- 但证书不受公有 CA 信任
- 浏览器仍会提示证书风险

### 4.2 防火墙层进一步收口

当前虽然 8080/8081 已改绑到 `172.17.0.1`，但服务器主机防火墙仍未启用。

这不影响本次“去公网暴露”的目标达成，但仍建议后续继续做：

- 启用 UFW 或等价防火墙
- 明确限制数据库、缓存、中间件的可达范围

### 4.3 Rust Node 业务健康问题

当前 `chain-node.service` 虽然运行中，但日志仍持续出现：

- `Block production failed: Parent block not found`

这说明：

- 节点入口已收口
- 但节点功能本身仍未健康

---

## 5. 最终结论

本次三项整改的结果如下：

- HTTPS：部分完成
- Go API 收口：完成
- Rust Node 收口：完成

更准确地说：

- 访问链路已经升级为 `HTTP -> HTTPS`
- 后端与节点端口已经不再直接暴露公网
- 但 HTTPS 目前仍是“自签证书可用态”，不是“正式受信任证书完成态”

当前部署状态建议更新为：

**“公网入口已收敛到 OpenResty，应用端口已完成内网收口，HTTPS 已启用但正式证书待补。”**
